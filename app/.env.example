# PROJECT SAINT — Environment Configuration
# Copy this to .env.local for development

# Auth Mode: 'demo' | 'clerk'
NEXT_PUBLIC_AUTH_MODE=demo

# Clerk (production auth — leave blank for demo mode)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Mapbox (territory maps)
NEXT_PUBLIC_MAPBOX_TOKEN=

# Groq (opportunity classification — mock in demo)
GROQ_API_KEY=

# Stripe (billing — mock in demo)
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# ShopDrawings.AI (mock in demo)
SHOP_DRAWINGS_AI_API_URL=

# App Configuration
NEXT_PUBLIC_APP_NAME=PROJECT SAINT
NEXT_PUBLIC_APP_URL=http://localhost:3000
